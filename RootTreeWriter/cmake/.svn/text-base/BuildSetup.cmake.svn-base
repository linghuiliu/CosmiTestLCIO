#############################################################################
# cmake build setup for Marlin
#
# For building Marlin with cmake type:
# (1) $ mkdir build
# (2) $ cd build
# (3) $ cmake -C ../BuildSetup.cmake ..
# (4) $ make install
#
# @author Jan Engels, DESY
#############################################################################



#############################################################################
# Setup path variables
#############################################################################

# ILC_HOME
SET( ILC_HOME "/afs/desy.de/group/it/ilcsoft"
    CACHE PATH "Path to ILC Software" FORCE )

# CMake Modules Path
SET( CMAKE_MODULE_PATH "${ILC_HOME}/CMakeModules/v01-01"
    CACHE PATH "Path to CMake Modules" FORCE )

# Path to LCIO
SET( LCIO_HOME "${ILC_HOME}/lcio/v01-09-pre2"
    CACHE PATH "Path to LCIO" FORCE )

SET( Marlin_HOME "${ILC_HOME}/Marlin/v00-09-09"
     CACHE PATH "Path to Marlin" FORCE )

SET( ROOT_HOME "${ILC_HOME}/root/5.16.00"
    CACHE PATH "Path to ROOT" FORCE )

