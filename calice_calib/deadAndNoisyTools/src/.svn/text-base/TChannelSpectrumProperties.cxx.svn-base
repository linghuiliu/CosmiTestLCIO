#include "TChannelSpectrumProperties.h"

ClassImp( TChannelSpectrumProperties )

TChannelSpectrumProperties::TChannelSpectrumProperties( Int_t module,
							Int_t chip,  Int_t channel,
							Double_t mean, Double_t rms)
: _module(module), _chip(chip), _channel(channel), _mean(mean), _rms(rms)
{}

void TChannelSpectrumProperties::Set( Int_t module, Int_t chip,   Int_t channel,
				      Double_t mean, Double_t rms)
{
  _module   = module;
  _chip     = chip;
  _channel  = channel;
  _mean     = mean;
  _rms      = rms;
}
